# ASCII value of n=78 and z=90
for i in range(ord('n'), ord('z') + 1):
    # Integer i with chr() will be converted to character
    # before printing. chr() will take its equivalent
    # character value
    print(chr(i), end=" ")